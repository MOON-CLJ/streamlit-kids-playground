[project]
name = "streamlit-kids-playground"
version = "0.1.0"
description = "A collection of simple, child-friendly games built with Streamlit"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.40,<2",
]

[tool.uv]
package = false
